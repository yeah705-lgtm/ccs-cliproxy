# Copy to .env and fill values
# SECURITY__AUTH_TOKEN is the shared API token required by ccproxy-api
SECURITY__AUTH_TOKEN=REDACTED_CHANGE_ME

# Optional logging overrides
LOGGING__LEVEL=INFO
LOGGING__FORMAT=json

# Optional UID/GID mapping
PUID=1000
PGID=1000
